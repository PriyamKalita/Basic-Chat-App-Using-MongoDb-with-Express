<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Chat</title>
</head>
<body>
    <p>Edit You Chat <%= chat._id %></p>
    <p>This Chat was from <%= chat.from %> to <%= chat.to %></p>

    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="msg" rows="20" cols="50"><%= chat.msg %></textarea><br><br><br>
        <button>Edit</button>
    </form>
</body>
</html>